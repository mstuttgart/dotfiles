# fix terminal colors
set -g default-terminal "${TERM}"
# set -g default-terminal "alacritty"
# set -ag terminal-overrides ",alacritty:RGB"

set -g default-terminal "screen-256color"
set -ga terminal-overrides ",xterm-256color:RGB"

# enable mouse
set -g mouse on

# change prefix
unbind C-b
set -g prefix C-Space
bind C-Space send-prefix

# resizing tmux panes
bind -r j resize-pane -D 5
bind -r k resize-pane -U 5
bind -r l resize-pane -R 5
bind -r h resize-pane -L 5

# maximizing and minimizing tmux pane
bind -r m resize-pane -Z

# Start windows a nd panes at 1, not 0
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# set vi-mode
set-window-option -g mode-keys vi

# # vim copy mode
bind-key -T copy-mode-vi 'v' send -X begin-selection # start selecting text with "v"
bind-key -T copy-mode-vi 'y' send -X copy-selection # copy text with "y"

# # don't exit copy mode after dragging with mouse
unbind -T copy-mode-vi MouseDragEnd1Pane

# # use xclip to copy and paste with the system clipboard
bind C-c run "tmux save-buffer - | xclip -i -sel clip"
bind C-v run "tmux set-buffer $(xclip -o -sel clip); tmux paste-buffer"

# split panes using | and -
bind | split-window -hc "#{pane_current_path}"
bind - split-window -vc "#{pane_current_path}"
unbind '"'
unbind %

# restore clean screen
bind C-l send-keys 'C-l'

# # automatically set the window title
set-window-option -g automatic-rename off

# # reload config file (change file location to your the tmux.conf you want to use)
bind r source-file ~/.tmux.conf \; display-message "Tmux config reloaded!"

# List of plugins
# tmux plguin manager
set -g @plugin 'tmux-plugins/tpm'

# tmux sane defaults
set -g @plugin 'tmux-plugins/tmux-sensible'

# Easy way to reset all options to default in tmux
set -g @plugin 'hallazzang/tmux-reset'

# for navigating panes and vim/nvim with Ctrl-hjkl
set -g @plugin 'christoomey/vim-tmux-navigator'

# enables copying highlighted text to system clipboard
set -g @plugin 'tmux-plugins/tmux-yank'

# persist tmux sessions after computer restart
set -g @plugin 'tmux-plugins/tmux-resurrect'

# automatically saves sessions for you every 15 minutes
set -g @plugin 'tmux-plugins/tmux-continuum'

# use zoxide to navegate through sessions
set -g @plugin 'jeffnguyen695/tmux-zoxide-session'

## COLORSCHEME: everforest dark medium
# set-option -g status "on"
# set -g status-interval 2

# set-option -g status-fg '#D3C6AA' # fg
# set-option -g status-bg '#3D484D' # bg0

# set-option -g mode-style fg='#D699B6',bg='#514045' # fg=purple, bg=bg_visual

# # default statusbar colors
# set-option -g status-style fg='#D3C6AA',bg='#232A2E',default # fg=fg bg=bg_dim

# # ---- Windows ----
# # default window title colors
# set-window-option -g window-status-style fg='#56635F',bg='#3D484D' # fg=yellow bg=bg0

# # default window with an activity alert
# set-window-option -g window-status-activity-style bg=colour237,fg=colour248 # bg=bg1, fg=fg3

# # active window title colors
# set-window-option -g window-status-current-style fg='#D3C6AA',bg='#425047' # fg=fg bg=bg_green

# # ---- Pane ----
# # pane borders
# set-option -g pane-border-style fg='#343F44' # fg=bg1
# set-option -g pane-active-border-style fg='#7FBBB3' # fg=blue

# # pane number display
# set-option -g display-panes-active-colour '#A7C080' # blue
# set-option -g display-panes-colour '#E69875' # orange

# # ---- Command ----
# # message info
# set-option -g message-style fg='#E67E80',bg='#232A2E' # fg=statusline3 bg=bg_dim

# # writing commands inactive
# set-option -g message-command-style fg=colour223,bg=colour239 # bg=fg3, fg=bg1

# # ---- Miscellaneous ----
# # clock
# set-window-option -g clock-mode-colour '#7FBBB3' #blue

# # bell
# set-window-option -g window-status-bell-style fg='#3D484D',bg='#E67E80' # fg=bg, bg=statusline3

# # ---- Formatting ----
# set-option -g status-left-style none
# set -g status-left-length 60
# set -g status-left "#[fg=#3D484D,bg=#A7C080,bold] #S #[fg=#A7C080,bg=#3D484D,nobold]#[fg=#A7C080,bg=#3D484D,bold] #(whoami) #[fg=#3D484D,bg=#3D484D,nobold]"

# set-option -g status-right-style none
# set -g status-right-length 150
# set -g status-right "#[fg=#3D484D,bg=#A7C080]#[fg=#3D484D,bg=#A7C080] #[fg=#3D484D,bg=#A7C080]%Y-%m-%d  %H:%M #[fg=#83C092,bg=#3D484D,bold]#[fg=#3D484D,bg=#83C092,bold] #h "

# set -g window-status-separator '#[fg=#9DA9A0,bg=#3D484D] '
# set -g window-status-format "#[fg=#9DA9A0,bg=#3D484D, bold] #I  #[fg=#9DA9A0,bg=#3D484D]#W "
# set -g window-status-current-format "#[fg=#3D484D,bg=#A7C080]#[fg=#3D484D,bg=#A7C080] #I  #[fg=#3D484D,bg=#A7C080,bold]#W #[fg=#A7C080,bg=#3D484D,nobold]"

# vim: ft=tmux
set -g mode-style "fg=#3D484D,bg=#A7C080"

set -g message-style "fg=#3D484D,bg=#A7C080"
set -g message-command-style "fg=#3D484D,bg=#A7C080"

set -g pane-border-style "fg=#A7C080"
set -g pane-active-border-style "fg=#3D484D"

set -g status "on"
set -g status-interval 1
set -g status-justify "left"

set -g status-style "fg=#3D484D,bg=#A7C080"

set -g status-bg "#3D484D"

set -g status-left-length "100"
set -g status-right-length "100"

set -g status-left-style NONE
set -g status-right-style NONE

set -g status-left "#[fg=#232A2E,bg=#9DA9A0,bold] #S #[fg=#9DA9A0,bg=#3D484D,nobold,nounderscore,noitalics] "
set -g status-right "#[fg=#3D484D,bg=#3D484D,nobold,nounderscore,noitalics]#[fg=#9DA9A0,bg=#3D484D]#[fg=#56635F,bg=#3D484D,nobold,nounderscore,noitalics]#[fg=#9DA9A0,bg=#56635F]#[fg=#9DA9A0,bg=#56635F,nobold,nounderscore,noitalics]#[fg=#15161E,bg=#9DA9A0,bold] #h "

setw -g window-status-activity-style "underscore,fg=#9DA9A0,bg=#3D484D"
setw -g window-status-separator ""
setw -g window-status-style "NONE,fg=#9DA9A0,bg=#3D484D"
setw -g window-status-format '#[fg=#3D484D,bg=#3D484D]#[default] #I > #{b:pane_current_path} #[fg=#3D484D,bg=#3D484D,nobold,nounderscore,noitalics]'
setw -g window-status-current-format '#[fg=#3D484D,bg=#eee8d5]#[fg=#A7C080,bg=#eee8d5] #I #[fg=#eee8d5,bg=#A7C080] #{b:pane_current_path} #[fg=#A7C080,bg=#3D484D,nobold]'


# Restore Neovim sessions
set -g @resurrect-strategy-nvim 'session'

# Restore Panes
set -g @resurrect-capture-pane-contents 'on'

# Start tmux Automatically on Boot
set -g @continuum-boot 'on'
set -g @continuum-boot-options 'alacritty'

# Set continuum save interval
set -g @continuum-save-interval '5'

set -g @continuum-restore 'on'

# Continuum status in tmux status line
# set -g status-right 'Continuum status: #{continuum_status}'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.config/tmux/plugins/tpm/tpm'

